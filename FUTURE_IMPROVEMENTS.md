# 今後の改善計画 (Future Improvements)

現在のプロジェクト状態から、さらなる品質向上、保守性の改善、ユーザー体験の強化を目的とした改善可能な箇所をまとめました。

## 1. コードアーキテクチャと保守性の向上 (Refactoring & Architecture)

### 1.1 `Main.java` の肥大化解消（シーン管理の分離）
現在、`Main.java` にスタート、コンフィグ、ゲーム、ゲームオーバー、エンドクレジットのすべてのシーン生成と切り替えロジックが集中しています。
* **改善案**: `SceneManager` パターンを導入し、各シーン（`StartScene`, `GameScene` 等）を別クラスに分割して、画面遷移を管理する専用のマネージャーを作成する。

### 1.2 `GameController.java` の責務分割
ゲームの進行ロジック、入力管理（DAS/ARR等）、SRS（スーパーローテーションシステム）のキックテーブル管理が一つのクラスにまとまっています。
* **改善案**: `InputHandler`（キー入力・長押し判定）、`RotationSystem`（SRSのキックテーブルと回転判定）、`ScoreManager`（スコア計算・レベル管理）に責務を分割し、テストを容易にする。

### 1.3 CSS の外部化
現在、`HudPane` や `Main` クラス内で `-fx-font-size: ...; -fx-background-color: ...;` といったスタイルが Java コード内にハードコードされています。
* **改善案**: `style.css` を導入してデザイン定義を外部ファイルに抽出し、Java 側は `getStyleClass().add("score-label")` のように論理的なクラス名のみを指定する。これによりデザインの変更・微調整が飛躍的に簡単になります。

---

## 2. ゲームプレイと操作性の進化 (Gameplay & Mechanics)

### 2.1 T-Spin 判定と Ren (コンボ) システム
現在のスコア計算はライン数のみに基づいています。現代的なパズルゲームとして、プレイヤースキルに応じた報酬を追加します。
* **改善案**: 
  - T-Spin (Mini / 通常) の判定ロジック実装と、専用のスコア倍率の適用。
  - 連続してラインを消した際のコンボ (Ren) カウンターの実装。
  - これらが成功した際に「T-SPIN DOUBLE!」などの派手なポップアップ演出を `HudPane` 等に出す。

### 2.2 キーコンフィグと入力カスタマイズ
操作が矢印キーや Z, X, SPACE 等に固定されています。
* **改善案**: `ConfigPane` にキーコンフィグ画面を追加し、プレイヤーが自分の好きなキーボード配列（WASDなど）にカスタマイズできるようにする。

### 2.3 ポーズ（一時停止）機能
ゲーム中に席を立つことができません。
* **改善案**: `ESC` または `P` キーでゲームの進行（`GameLoopTimer`）を停止し、画面上にポーズメニュー（再開 / リトライ / タイトルへ戻る）を表示する。再開時には「3, 2, 1」のカウントダウン演出を入れる。

---

## 3. UI/UX と演出のさらなる洗練 (UI/UX Polish)

### 3.1 BGMのフェード処理とUI効果音
ゲームオーバー時やシーン切り替え時に BGM が `stopBgm()` で即座に切れるため、やや唐突な印象を与えます。
* **改善案**: `Timeline` などを利用して、数秒かけて BGM の音量を絞るフェードアウト処理を追加する。また、メニュー画面でのカーソル移動や決定音などの UI 効果音を追加する。

### 3.2 ゴーストミノとハードドロップ演出
現在のゴーストミノは不透明度を下げたグレーのブロックですが、よりスタイリッシュにする余地があります。
* **改善案**: ゴーストミノを「ブロックの輪郭（ボーダー）のみ」で描画するように変更する。また、ハードドロップ時に上から下へ一瞬光の筋が残る（落下軌跡エフェクト）を追加する。

### 3.3 盤面の揺れ（スクリーンシェイク）
* **改善案**: 4ライン消去（テトリス）や重力反転ギミック（ワールド回転）が発動した際に、`DeviceFramePane` 全体を小さく振動させるスクリーンシェイク（画面揺らし）演出を追加して、インパクトを強める。

---

## 4. データとシステム (System & Persistence)

### 4.1 設定・ハイスコアの保存 (Save Data)
音量などの設定を起動のたびにやり直す必要があります。
* **改善案**: `GameConfig` クラスの内容や、これまでの自己ベストスコア（ローカルハイスコアランキング）を `config.json` や `save.dat` として保存し、次回起動時に読み込む仕組みを構築する。

### 4.2 リプレイ機能
* **改善案**: プレイヤーのキー入力履歴と乱数シードを記録し、ゲーム終了後に自分のプレイを見直すことができるリプレイ機能を実装する。
